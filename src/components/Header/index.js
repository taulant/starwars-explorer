const Header = (props) => {
  const className = "Header";

  return (
    <header className={className}>
      <h1 className="text-3xl mb-4">Star Wars Explorer</h1>
    </header>
  );
};

export default Header;
